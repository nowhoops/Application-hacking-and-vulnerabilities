# H5.md
| For this task pc specs | vm/os |
|---------|-------------|
| (I7/128/8tb/) | Fedora |

Started at 14:00 Ended 22:00 Feb 7th

h5 It's Alive! (Lari)

    a) Lab1. Investigate what's wrong with the program and how to fix it. lab1.zip(https://terokarvinen.com/application-hacking/lab1.zip)

    With command unzip lab1.zip unziped the file 
    with cd lab1 went tols  the directrory  

    Task is to go thrue the files with gnu debugger. 
    Lets start with
    gdb ./gdb_example1
    I run the command start and after that I ran the command step,. It startd to go thrue all like malloc.c, fileops.c, sbrk.c and libioP.h witch is not what we want.

    Checked the correct commadn and it is "step" or "s".
    with that I got this command 
    
Temporary breakpoint 2, main () at gdb_example1.c:14
14	  char * bad_message = NULL;
(gdb) next
15	  char * good_message = "Hello, world.";
(gdb) 
17	  print_scrambled(good_message);
(gdb) 
Khoor/#zruog1
18	  print_scrambled(bad_message);
(gdb) 

Program received signal SIGSEGV, Segmentation fault.
0x000055555555514f in print_scrambled (message=0x0) at gdb_example1.c:7
7	    printf("%c", (*message)+i);
(gdb) 
.

So I think there is something wrong with the print_scrambled(bad_message); task.

It fails when it gets to  this point Program received signal SIGSEGV, Segmentation fault.
0x000055555555514f in print_scrambled (message=0x0) at gdb_example1.c:7
7	    printf("%c", (*message)+i);

The code is probably 

void print_scrambled(char *message) {
    register int i = 3;
    do {
        printf("%c", (*message) + i);
    } while (*++message);
    printf("\n");
}
with this i notice that it tries to scrable  Null witch is not a string.
We have few ways of  fixing it we can make a check if null print that it is null or. Make the NULL a string

IT is pit confusing what to do. We dont have instrunktinos on how to fix it. We don't have the "right way" it shuold run.
fix 1
chell@fedora:~/Downloads/lab1$ cat gdb_fix 
#include <stdio.h>

void print_scrambled(char *message)
{
  register int i = 3;
  do {
    printf("%c", (*message)+i);
  } while (*++message);
  printf("\n");
}

int main()
{
  char * bad_message = "NULL";
  char * good_message = "Hello, world.";

  print_scrambled(good_message);
  print_scrambled(bad_message);
}
chell@fedora:~/Downloads/lab1$ cp gdb_fix gdb_fix.c
chell@fedora:~/Downloads/lab1$ gcc -g -o gdb_fix gdb_fix.c
chell@fedora:~/Downloads/lab1$ ./gdb_fix
Khoor/#zruog1
QXOO





    
    b) Lab2. Find out the password and flag + write a report on how it opened. lab2.zip(https://terokarvinen.com/application-hacking/lab2.zip)
    c) Lab3. Try Nora Crackmes exercises tasks 3 and 4; the rest are optional. Tindall 2023: NoraCodes / crackmes.(https://github.com/NoraCodes/crackmes)
    d) Lab4. Optional: Crackmes.one(https://crackmes.one/) exercise. Can you find out the password? lab4.zip in Moodle.

Tips

    lab0.zip solved in class
    Lari's slides are available in Moodle
    
gdb QuickStart: https://web.eecs.umich.edu/~sugih/pointers/gdbQS.html
https://terokarvinen.com/application-hacking/lab1.zip
https://terokarvinen.com/application-hacking/lab2.zip
https://terokarvinen.com/application-hacking/lab3.zip
https://terokarvinen.com/application-hacking/lab4.zip
