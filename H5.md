# H5
| For this task pc specs | vm/os |
|---------|-------------|
| (I7/128/8tb/) | Fedora |

Started at 12:00 Ended 21:00 Feb 21th



# Lab 1 – Debugging With GDB  
Investigating what’s wrong with the program and how to fix it.  
Original file: **lab1.zip** (https://terokarvinen.com/application-hacking/lab1.zip)

---

### Unzipping and Exploring the Lab
```  
unzip lab1.zip  
cd lab1  
ls  
```

The task is to go through the files using **GNU Debugger (gdb)**.

---

### Starting GDB
```  
gdb ./gdb_example1  
start  
next  
```

Using `next` keeps execution inside your own code instead of stepping into system libraries.

---

## :play Running the Program in GDB
Program flow:

```
Temporary breakpoint 2, main () at gdb_example1.c:14
14    char * bad_message = NULL;
(gdb) next
15    char * good_message = "Hello, world.";
(gdb) next
17    print_scrambled(good_message);
(gdb) next
Khoor/#zruog1
18    print_scrambled(bad_message);
(gdb) next

Program received signal SIGSEGV, Segmentation fault.
0x000055555555514f in print_scrambled (message=0x0) at gdb_example1.c:7
7        printf("%c", (*message)+i);
```

---

## Root Cause
The function is likely:

```  
void print_scrambled(char *message) {  
    register int i = 3;  
    do {  
        printf("%c", (*message) + i);  
    } while (*++message);  
    printf("\n");  
}  
```
**I checked it it was correct**

The crash occurs because:

- `bad_message = NULL`  
- it breaks the function and causes a **segmentation fault**.

---
There were no clear explenation on what a fixed code should do. I found two fixes that don't break the code. 

### Fix Option 1 — Make the NULL a String

```  
#include <stdio.h>

void print_scrambled(char *message)
{
  register int i = 3;
  do {
    printf("%c", (*message)+i);
  } while (*++message);
  printf("\n");
}

int main()
{
  char * bad_message = "NULL";
  char * good_message = "Hello, world.";

  print_scrambled(good_message);
  print_scrambled(bad_message);
}
```

Output:

```
Khoor/#zruog1
QXOO
```

---

### Fix Option 2 —  IF NULL then message

```  
#include <stdio.h>

void print_scrambled(char *message)
{
    if (message == NULL) {
        printf("ERROR: NULL message\n");
        return;
    }

    int i = 3;
    do {
        printf("%c", (*message) + i);
    } while (*++message);
    printf("\n");
}

int main()
{
  char * bad_message = NULL;
  char * good_message = "Hello, world.";

  print_scrambled(good_message);
  print_scrambled(bad_message);
}
```

Output:

```
Khoor/#zruog1
ERROR: NULL message
```

---

## SUMMARY
- The crash occurs because `print_scrambled()` does not work with a NULL pointer.  
- Two valid fixes:  
  - Replace NULL with a real string  
  - Add a NULL check inside the function  




    
# Lab 2 – Password & Flag Recovery Report  
Source: **lab2.zip** (https://terokarvinen.com/application-hacking/lab2.zip)

---

### Getting Started
```  
wget https://terokarvinen.com/application-hacking/lab2.zip  
unzip lab2.zip  
cd ~/Downloads/lab2/passtr  
```

The README explains that there are **two binaries**:

- `passtr` — includes source code  
- `passtr20` — stripped, no source, harder challenge  

---

## Part 1 — Cracking `passtr`

### Running in GDB
```  
gdb passtr
start
Next
```

I stepped through the program until it asked for a password.  
I entered something random, and then saw the comparison:

```
if (0 == strcmp(password, "sala-hakkeri-321"))
```

So the password is:

```
sala-hakkeri-321
```

Running the program normally and entering the correct password prints the **flag**.

<img width="776" height="165" alt="Screenshot From 2026-02-21 16-03-00" src="https://github.com/user-attachments/assets/7ffd0a81-c2dd-4e60-8166-0be7845b2bef" />

---

### Part 2 — Cracking `passtr2o`

This binary is much harder. It keeps the password in the memmory somehow. 

Running it normally:

- It immediately prints **“Sorry, no bonus.”**

So the password is **not** visible in memory during normal execution. Even with the same commands as in the part 1. Skips from user input to **“Sorry, no bonus.”**




---

###  Step 1 — Inspecting Functions

I noticed a function at address while going true the code, labeled by GDB as:

```
mAsdf3a
```

This is likely the password‑checking function.

I set a breakpoint:

```  
break mAsdf3a  
run  
```

Then I inspected the disassembly:

<img width="510" height="416" alt="Screenshot From 2026-02-21 16-58-46" src="https://github.com/user-attachments/assets/d1485155-cc8d-41a2-bd95-4b420b19cecf" />
<img width="664" height="623" alt="Screenshot From 2026-02-21 16-58-59" src="https://github.com/user-attachments/assets/a76f502f-3e7e-4e35-b833-9920d2b55e9f" />



I saw a suspicious string:

```
anLTj4u8
```

I tried it as a password — **not correct**.

---

### Step 2 — Hunting  `strcmp`

I set a breakpoint on `strcmp`:

```  
break main  
run  
break strcmp  
continue  
```

Then I entered a test password like:

```
AAA
```

I inspected the arguments:

```  
x/s $rdi  
x/s $rsi  
```

But the values were things like:

- `"strlen"`
- `"GLIBC_2.2.5"`
Witch were not the correct passwords.

---

#  Step 3 — Inspecting `main` Logic

I disassembled `main`:

```  
disassemble main  
```
<img width="630" height="653" alt="image" src="https://github.com/user-attachments/assets/6f18e52f-b244-4cc0-96ed-5fc229dcd1b1" />

I identified the key instruction:

```
dec %eax
```

I discovered that the instruction dec %eax controls the jump:
if %eax something jump




---

### Step 4 — Forcing the jump

I set a breakpoint at the instruction:

```  
break *0x555555555105  
run  
```

At the breakpoint, I tried:


```  
set $eax = 0  
continue  
```

This still went to **“Sorry, no bonus.”**
so I tried forcing others but while reading a stackoverflow chat realized that I need to set it to 1 and not 0

```  
set $eax = 1  
continue  
```

This time it **jumped to the success branch** and printed the **flag**.
<img width="550" height="442" alt="Screenshot From 2026-02-21 17-14-45" src="https://github.com/user-attachments/assets/af929ba7-ba8a-4829-9aa8-8a6d2dc92f97" />
- So found the flag and not the pass

---

### Final Result

- **`passtr`**: Password found directly in GDB → `sala-hakkeri-321`  
- **`passtr2o`**: Password not recovered, but flag obtained with Runtime manipulatio
---

## c) 
Lab3. Try Nora Crackmes exercises tasks 3 and 4;
Source: https://github.com/NoraCodes/crackmes

---

## Crackme03

I built the **crackme03** binary and ran it in gdb.  
After doing `start` and a few `next` steps, I saw this:

```
32     char correct[CORRECT_LEN + 1] = "lAmBdA";
33     char mask[CORRECT_LEN + 1] = {2, 3, 2, 3, 5};
36     if (strlen(argv[1]) == CORRECT_LEN && check_pw(argv[1], correct, mask)) {
41     printf("No, %s is not correct.\n", argv[1]);
No, ̈́ is not correct.
42     return 1;
```

So `"lAmBdA"` is **not** the password.  
The code modifies it using the mask array.  
I calculated the transformed characters:

| i | correct[i] | ASCII | mask[i] | Result ASCII | Result Char |
|---|------------|--------|----------|---------------|--------------|
| 0 | 'l'        | 108    | +2       | 110           | 'n'          |
| 1 | 'A'        | 65     | +3       | 68            | 'D'          |
| 2 | 'm'        | 109    | +2       | 111           | 'o'          |
| 3 | 'B'        | 66     | +3       | 69            | 'E'          |
| 4 | 'd'        | 100    | +5       | 105           | 'i'          |
| 5 | 'A'        | 65     | +0       | 65            | 'A'          |

So the password becomes:

```
nDoEiA
```

<img width="395" height="43" alt="image" src="https://github.com/user-attachments/assets/559a58c2-504a-46d4-974e-b88c3f067bac" />

---

## Crackme04

For crackme04 I did the same style as with passtr2o.  
I found the interesting line:

```
29     correct = (i == CORRECT_LEN) && (sum == CORRECT_SUM);
```

I set a breakpoint:

```
break crackme04.c:24
disassemble
```

Changing registers didn’t work this time, so I brute‑forced the jump.  
I opened another terminal to keep the disassembly visible and looked at this block:

```
0x00000000004004db <+101>: mov    (%rax),%rdx
0x00000000004004de <+104>: mov    -0x4(%rbp),%eax
0x00000000004004e1 <+107>: cltq
0x00000000004004e3 <+109>: add    %rdx,%rax
0x00000000004004e6 <+112>: movzbl (%rax),%eax
0x00000000004004e9 <+115>: test   %al,%al
0x00000000004004eb <+117>: jne    0x4004b3 <main+61>
0x00000000004004ed <+119>: cmpl   $0x10,-0x4(%rbp)
0x00000000004004f1 <+123>: jne    0x400503 <main+141>
0x00000000004004f3 <+125>: cmpl   $0x6e2,-0x8(%rbp)
0x00000000004004fa <+132>: jne    0x400503 <main+141>
0x00000000004004fc <+134>: mov    $0x1,%eax
0x0000000000400501 <+139>: jmp    0x400508 <main+146>
0x0000000000400503 <+141>: mov    $0x0,%eax
```

The success path is clearly at:

```
0x4004fc
```

So I forced execution to jump there:

```
start
jump *0x4004fc
```

And got the answer:

<img width="763" height="188" alt="image" src="https://github.com/user-attachments/assets/e5ec5af3-6a34-452a-a19b-1ae26aa7515e" />

---

Then I realized this wasn’t the actual task. We’re supposed to **find the password**, **LOL**.  
So I went back and started digging again.

I tried printing CORRECT_LEN and CORRECT_SUM, but they didn’t print.  
everything else printed. So they might be macros → meaning they’re somewhere in the assembly.

found a comparisons (`cmpl`) and found these:

```
0x0000000000400489 <+19>:  cmpl   $0x2,-0x14(%rbp)
0x00000000004004ed <+119>: cmpl   $0x10,-0x4(%rbp)
0x00000000004004f3 <+125>: cmpl   $0x6e2,-0x8(%rbp)
```

The last two are right next to each other, so I assumed:

- `0x10` is CORRECT_LEN  
- `0x6e2` is CORRECT_SUM  

Checked them in Python:

```
>>> 0x6e2
1762
>>> 0x10
16
```

So the password must be:

- length = **16**
- sum of ASCII values = **1762**

I forced the i and the sum to those values in gdb:

<img width="865" height="399" alt="image" src="https://github.com/user-attachments/assets/3f64f890-60c0-4b6b-9887-f9c6440ee1ce" />

Now I needed a 16‑character string with ASCII sum is 1762.  

---
I tried but couldn’t figure it out, so I asked Lumo AI:

<img width="785" height="626" alt="image" src="https://github.com/user-attachments/assets/7a3306cc-8a07-4dff-8260-c0a16e872dbd" />

It suggested:

```
nnnnnnnnnnnnnnnp
```

I checked it:

<img width="871" height="100" alt="image" src="https://github.com/user-attachments/assets/b69dd4c2-2c14-4c71-a65e-254143cbdd2d" />

And yes — **that is correct**.

So the password for crackme04 is:

```
nnnnnnnnnnnnnnnp
```



sources:


https://terokarvinen.com/application-hacking/    
gdb QuickStart: https://web.eecs.umich.edu/~sugih/pointers/gdbQS.html
https://terokarvinen.com/application-hacking/lab1.zip
https://terokarvinen.com/application-hacking/lab2.zip
https://terokarvinen.com/application-hacking/lab3.zip
https://terokarvinen.com/application-hacking/lab4.zip
https://www.reddit.com/r/Assembly_language/comments/1855gmj/what_does_edxecxebx_or_eax_mean_in_assembly/?tl=fi
https://stackoverflow.com/questions/13064809/the-point-of-test-eax-eax
https://stackoverflow.com/questions/24118562/the-difference-between-cmpl-and-cmp
https://lumo.proton.me
